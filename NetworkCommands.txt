List all ports being used:
  netstat -a
  
IP address of the domain server
  nslookup www.google.com
  
#Check the ARP cache
arp -a
arp -s ipaddress macaddress  -- Creates a static entry in local machine arp cache.
